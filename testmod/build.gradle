// :testmod — test addon. Depends ONLY on :api so the SPI is exercised end-to-end.

plugins {
    id 'colony.mod-conventions'
}

dependencies {
    implementation project(':api')
}

neoForge {
    mods {
        colonytest {
            sourceSet sourceSets.main
        }
    }
}
